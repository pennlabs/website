import React from 'react'

import { H1 } from '../../shared'
import { Links } from './Links'

const Nav = (): React.ReactElement => (
  <nav>
    <H1>Logo</H1>
    <Links />
  </nav>
)

export default Nav
